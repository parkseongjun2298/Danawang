from distutils.core import setup

setup(name = "Danawang",
      version = '1.0',
      packages = ['TermProject', 'TermProject.image'],
      package_data = {'TermProject' : ['*'], 'TermProject.image' : ['*']},
      )
